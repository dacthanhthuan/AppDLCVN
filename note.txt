(Initial API) referral_link_multiple_enabled == 1 ? 'Hiển thị QR code trong chia sẻ' : 'Hiển thị link chia sẻ không có QR code'
(client Referral) descriptions: hiển thị đầu tiên, referral_link: danh sách hiển thị, render QR bằng uri: render_qrcode + referral_link
