import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    containerView: {
        width: '100%',
        borderColor: '#C2C2C2',
        borderWidth: 1,
        borderRadius: 10,
        padding: 12,
        marginBottom: 8
    }
})

export default styles;