import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  containerLoading: {
    opacity: 0.5,
  },

  indicatorView: {
    alignSelf: 'center',
    position: 'absolute',
    zIndex: 5,
    top: 150,
  },
});

export default styles;
