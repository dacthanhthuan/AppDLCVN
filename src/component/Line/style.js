import { StyleSheet } from "react-native";

const Style_Line = StyleSheet.create({
    line: {
        borderWidth: 0.2,
        borderColor: '#808080',
        width: '100%',
        marginTop: 15,
        marginBottom: 15,
    },
});

export default Style_Line;