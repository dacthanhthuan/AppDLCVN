import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  list: {
    flex: 1,
  },

  listContent: {
    paddingVertical: 10,
    gap: 10,
  },

  emptyListText: {
    fontSize: 20,
    fontWeight: '500',
    color: '#005aa9',
    alignSelf: 'center',
  },
});

export default styles;
