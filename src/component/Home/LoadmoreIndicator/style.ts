import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  progress: {
    height: 5,
  },

  text: {
    fontWeight: '600',
    fontSize: 13,
    color: 'black',
    textAlign: 'center',
  },
});

export default style;
