import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  container: {
    position: 'absolute',
    bottom: 0,
  },

  progress: {
    height: 5,
  },

  text: {
    fontWeight: '600',
    fontSize: 13,
    color: 'black',
    textAlign: 'center',
  },
});

export default style;
